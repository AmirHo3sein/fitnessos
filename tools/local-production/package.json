{
  "name": "@fitnessos/local-production",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "perf:authenticated": "node --experimental-strip-types lighthouse-authenticated.ts",
    "perf:public": "npx --yes @lhci/cli@0.15.1 collect --config=../../lighthouserc.local-production.cjs",
    "smoke": "playwright test --config smoke.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@fitnessos/config": "workspace:*",
    "@playwright/test": "^1.62.1",
    "typescript": "^5.6.3"
  }
}
